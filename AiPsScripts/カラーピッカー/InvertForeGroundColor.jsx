#target photoshopapp.bringToFront();var flipViaRGB = false;if (flipViaRGB) {    try{	        var fgColor = new SolidColor();		fgColor.rgb.red = 255 - app.foregroundColor.rgb.red;		fgColor.rgb.green = 255 - app.foregroundColor.rgb.green;		fgColor.rgb.blue = 255 - app.foregroundColor.rgb.blue;        app.foregroundColor = fgColor ;    }    catch(e){        ; // do nothing    }} else {    try{	        var fgColor = new SolidColor();		if (app.foregroundColor.hsb.hue >= 180) {            app.foregroundColor.hsb.hue = app.foregroundColor.hsb.hue - 180;         } else {             app.foregroundColor.hsb.hue = app.foregroundColor.hsb.hue + 180;         }        app.foregroundColor = fgColor ;    }    catch(e){        ; // do nothing    }}