#target "photoshop"var text = prop($.global);var dir = File.saveDialog("ログのtxtファイルを保存します"); //すいませんが拡張子はセルフサービスで	if(dir){		var a = new File(dir);		a.open("w");		a.write(text);		a.close();		}//debugfunction prop(obj) {    var properties = '';    for (var prop in obj){        properties += prop + "=" + obj[prop] + "\n";    }    //alert(properties);    return properties;}   